Meteor.startup(function(){
  //Everything that looks like a Method to call on the server
  //Declare the methods on the server that can be accessed by the client
  Meteor.methods({
    sampleMethod: function(args) {
      
    }
  });  
}); 

